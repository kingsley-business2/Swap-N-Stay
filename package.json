# Recreate package.json cleanly
echo '{
  "name": "swap-n-stay",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prebuild": "node verify-files.cjs",
    "typecheck": "tsc --noEmit",
    "build": "npm run prebuild && npm run typecheck && vite build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "typescript": "^5.2.0",
    "@types/react": "^18.0.28",
    "@types/react-dom": "^18.0.11",
    "@types/react-hot-toast": "^2.4.0"
  }
}' > package.json

# Generate package-lock.json
npm install
