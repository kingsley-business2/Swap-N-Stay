// ========================== package.json (UPDATED) ==========================
{
  "name": "swap-n-stay",
  "version": "1.0.0",
  // ❌ REMOVED: "homepage" for GitHub Pages
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    // ❌ REMOVED: "predeploy" and "deploy" scripts
    
    // ➕ ADDED: New Capacitor scripts
    "capacitor:copy": "npx cap copy",
    "capacitor:open:ios": "npx cap open ios",
    "capacitor:open:android": "npx cap open android",
    "sync:native": "npm run build && npm run capacitor:copy"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    // ... your other dependencies
  },
  "devDependencies": {
    // ... your other devDependencies
    "typescript": "^5.2.2",
    "@capacitor/cli": "5.7.0",
    "@capacitor/core": "5.7.0",
    "@capacitor/android": "5.7.0",
    "@capacitor/ios": "5.7.0"
    // ❌ REMOVED: "gh-pages"
  }
}
